/*
a = 90
b = 99

console.log(a + b)
*/

function getnum(a, b) {
    var a = eval(input('Enter the number here: '));
    var b = eval(input('Enter the number2 here: '));

    console.log(a)
    console.log(b)
}

function add(x, y) {
    x = a;
    y = b;

    var sum = x + y;
    console.log(sum)
    return sum;
}

function display() {
    console.log('The sum of the two numbers is ', sum);
}

function over() {
    document.write("Mouse Over");
}

function out() {
    document.write("Mouse Out");
}